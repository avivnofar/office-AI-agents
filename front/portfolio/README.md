<!--
  SKELETON — structure only, no visitor-facing copy. See ../README.md.
  Section 2 of the owner's narrative order.

  THE BAR, and it is not negotiable: only deliverables at `CLIENT-READY` are
  reported as finished. `docs/CLIENT-REQUIREMENTS.md` reading 4. A deliverable
  still `IN-REVIEW` is not portfolio content — its lifecycle record
  (`warehouse-office-AI-agents/tasks/<slug>/STATE.json`) is the source of
  truth for that state, and "in progress" belongs on the internal board, not
  on a public portfolio.

  DRAWN FROM REAL OUTPUT, NOT FROM CLAIMS. Each entry cites the artifact it
  describes. An entry nobody can check against a file is a claim, and this
  section is the one a visitor will most reasonably be sceptical of.
-->

# What the office built

<!-- PLACEHOLDER — section introduction: what it means for an office of agents
     to have produced something, and what the review loop each item passed
     through actually was. -->

## Data Center

<!-- PLACEHOLDER — what the office did for [Data Center](https://avivnofar.github.io/data-center/):
     the questions asked, the answers judged, the capability gaps flagged back
     for review. Drawn from `reports/gaps/data-center/` and the guides
     pipeline's published output. -->

## Notebook-X

<!-- PLACEHOLDER — what the office did for [Notebook-X](https://notebook-x.vercel.app):
     same shape. Drawn from `reports/gaps/notebook-x/`. Note for whoever
     writes this: the office's findings here are mostly NEGATIVE — the
     notebooks it tested often returned nothing — and A10 says that publishes
     as it was recorded, in the same words. It is a real finding about a real
     system, which is more useful to a visitor than a success story. -->

## Archive Alpha

<!-- PLACEHOLDER — named, NOT linked, until it opens. Owner instruction. Do not
     add a URL to this section without his say-so. -->

## The guides

<!-- PLACEHOLDER — the technical guides the office wrote and fact-checked: one
     persona drafts, the Architect reviews against a live source, and anything
     that could not be verified is published marked UNVERIFIED rather than
     guessed. Real files: `guides/<domain>/<slug>.md`. The rejected drafts in
     `guides/_drafts/` belong here too — a rejection is the review loop
     working, and hiding it would make the approvals mean less. -->
