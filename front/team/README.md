<!--
  SKELETON — structure only, no visitor-facing copy. See ../README.md.
  Section 1 of the owner's narrative order, and the one he most wants a
  visitor to meet. It therefore carries the most weight and is dispatched
  first (board: OB-088).

  WHO WRITES WHAT. Each agent's page is written BY THAT AGENT where the
  character bible supports it, and edited by the Designer. An agent that does
  not run cannot write its own page — say so on the page rather than having
  another agent ghost-write it in that agent's voice. A page asserting
  authorship that did not happen is KFM-01, the audit's #1 finding, reproduced
  on the most public surface the project has.

  SOURCES: `AGENTS-CHARACTER-CORE-v2.md` (private, back-office — named, never
  linked), `config/agents-config.json` for what is actually wired, and
  `docs/CAPABILITY-TOOLBOX.md` for capability status per role.
-->

# The agents

<!-- PLACEHOLDER — section introduction. What an office of AI personas is, and
     why each one has its own logic rather than being one model in costume. -->

## The roster

<!-- PLACEHOLDER — one row per agent, linking to its page below. The count is a
     live fact and must be read from `config/agents-config.json` at the time of
     writing, never copied from a document: the config ships 13, and several
     office documents still say 11. Whichever number is true on the day this
     publishes, the page states it and states what "wired" means, because the
     gap between specified and running is exactly the kind of thing A10 says to
     publish rather than smooth over. -->

## Hierarchy and what a clearance tier gates

<!-- PLACEHOLDER — structural, not biographical: who reports to whom, who
     reviews whom, and what `standard` / `specialist` / `sudo` / `root`
     actually gate. This is where "hierarchy and office logic" is SHOWN rather
     than asserted. -->

## Per-agent pages

<!-- PLACEHOLDER — one file per agent in this directory, named
     `NN-the-<name>.md` to match the campus room layout
     (`campus/agents/09-the-designer/`), so a reader and a maintainer see the
     same identifier.

     Each page carries, at minimum:
       - role, tier, clearance
       - the agent's own logic: what it does, what it refuses, what it
         escalates and to whom
       - whether it RUNS today, stated plainly
       - where its voice comes from (its own writing, or the bible, or both)
-->
