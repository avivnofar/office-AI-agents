<!--
  SKELETON — structure only, no visitor-facing copy. See ../README.md.
  Section 3 of the owner's narrative order: the autonomy and the learning loop.

  THE HARDEST SECTION TO WRITE HONESTLY, and the instruction for it is
  explicit: describe the mechanism that exists, not the aspiration. "An office
  running a full week unattended" is a hope the owner NAMED AND EXCLUDED from
  the commitment (`docs/CLIENT-REQUIREMENTS.md` reading 4). A section that
  implies otherwise fails on its own subject matter.

  The office spent the week of 2026-08-10 discovering the difference between a
  mechanism that is built and one that is CONNECTED — several signature
  instruments turned out to be reachable only by a manual admin call, having
  never once run autonomously. Saying so makes this section stronger, not
  weaker: it is the review loop catching itself, which is the exact thing the
  section claims the office can do.
-->

# How the office reviews itself

<!-- PLACEHOLDER — section introduction: what "autonomous" concretely means
     here, stated as a mechanism a reader could check. -->

## The deliverable lifecycle

<!-- PLACEHOLDER — build → admin review → gaps to a meeting → discussion and a
     binding vote → improvement round → back to review → CEO approval.
     Eight stages, one forward exit, and that exit is the CEO. **No cap on
     rounds** — a deliverable going round without converging is a finding, not
     a reason to ship it. Source: `workers/deliverable-lifecycle.js`. -->

## The learning loop

<!-- PLACEHOLDER — what the office changes about itself after work happens:
     active-context amendment, probation, cross-embodiment comparison. State
     which of these RUN and which are built-but-unconnected, per
     `docs/CAPABILITY-TOOLBOX.md` at the time of writing — and read that state
     live rather than quoting any document, including this comment. -->

## What runs unattended, and what does not

<!-- PLACEHOLDER — the honest split. The office runs on a 30-minute cron during
     office hours; a nightly unattended session takes work off the board. Name
     what that has actually produced and what still needs a supervised session.
     A reader who finds one overstatement here will disbelieve the whole
     section, and rightly. -->

## Where it has been wrong

<!-- PLACEHOLDER — A10, applied to this section's own subject. The office has
     found real defects in its own instruments: a quality metric that measured
     length rather than quality and had already published a false finding; a
     sampler whose selection was 2.4% against a declared 12.5%; meeting
     transcripts that gave speaking lines to agents that do not run. Each was
     found by the office's own review, and each is published here in the words
     it was recorded in internally.

     NOT HERE: security findings, in any form — open, closed or historical.
     A10, no exceptions, and the scan on the write path enforces it. -->
