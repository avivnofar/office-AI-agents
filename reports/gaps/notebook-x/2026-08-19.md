# Capability gaps — notebook-x — 2026-08-19

5 capability gaps flagged today: 5 HARD (the tool produced nothing — no notebook coverage, or the request failed outright); 0 SOFT (an answer came back but scored below the flagging agent's own threshold).

> **HARD and SOFT are different findings and are deliberately not summed into one headline.** A HARD gap is evidence the tool failed. A SOFT gap is evidence an answer scored low — and the score is a length proxy, so a SOFT gap means the answer was SHORT, not that it was wrong. Quality scores here are a LENGTH PROXY, not a quality judgment: score = min(1, answer_characters / 800), scorer `length-proxy-v2@800`. No model reads the question or the answer to produce this number. A long wrong answer outscores a short right one, and any answer past the divisor scores 1.0 regardless of content. DIVISOR UNIFICATION (OB-080), effective 2026-08-16T00:00:00Z: until that instant the office ran TWO scorers — divisor 800 for data-center and 600 for notebook-x — so the same answer scored 33% higher on notebook-x. From that instant there is ONE divisor, 800, for every project. Scores are comparable across projects only from that date forward. Historical rows were NOT rescored and keep the numbers they were given (OFFICE-POLICY A15); any average pooling rows from both sides of the date is measuring the formula change as well as the thing.

Internal office notes — not GitHub Issues, not customer-facing.

## The Trainee — case `qa-2026-w08-d4-036` — HARD

ב-Notebook-X, במחברת ה-kb-linux, חסרה יכולת ניתוח לתהליכים חורגים (runaway process), מה שגרם למערכת להחזיר תשובה ריקה לחלוטין. זה מלמד שהכלי לא בנוי להתמודד עם תרחישי חירום בזמן אמת, ואני מרגיש שאני צריך דחוף מדריך PDF מסודר לטיפול ידני כי המערכת פשוט לא נותנת מענה.

---

## The Perfectionist — case `qa-2026-w08-d4-037` — HARD

ה-Notebook-X נכשל בהחזרת תשובה לשאלה על תהליך גילוי פגיעויות ב-kb-cybersecurity, מה שמעיד על חסר קריטי במיפוי נהלי עבודה מעשיים בסביבת ייצור. הכלי לא מספק תשתית תהליכית בסיסית, ואני מסמן זאת כפער יכולת הדורש עדכון של בסיס הידע בתחום ה-VDP וה-Incident Response.

---

## The Perfectionist — case `qa-2026-w08-d4-037` — HARD

Notebook-X נכשל בהפקת תשובה תחת ה-kb-cybersecurity למרות הרלוונטיות הגבוהה של הנושא ל-SDLC. חסרה אינדוקציה של תהליכי Disclosure בתוך ה-Knowledge Base, מה שמעיד על פער בכיסוי מתודולוגיות אבטחה מודרניות; דורש עדכון דחוף של מסדי הנתונים ב-Notebook.

---

## The Productive — case `qa-2026-w08-d4-038` — HARD

Notebook-X נכשל בשליפת פקודת `usermod -aG` בסיסית ב-kb-linux, חוסר יכולת מטריד בביצוע פעולות ניהול משתמשים סטנדרטיות. הכלי דורש אינדוקס מחדש של פקודות ניהול הרשאות מבוססות CLI כדי למנוע בזבוז זמן יקר על חיפושים ידניים. סימנתי לתיקון ב-back-office, חוסר יעילות כזה לא עובר אצלי.

---

## The Standard Agent — case `qa-2026-w08-d4-039` — HARD

במהלך אבחון תקלות SIP ב-Notebook-X, ה-Notebook הייעודי ל-1com לא סיפק מענה כלל לשאילתת הרישום, מה שמעיד על דלות בבסיס הידע הטכני המובנה שלו. הפער הזה מחייב הרחבה של תרחישי ה-Troubleshooting בתיעוד הפנימי כדי למנוע הישענות על ניחושים במצבי קצה.
