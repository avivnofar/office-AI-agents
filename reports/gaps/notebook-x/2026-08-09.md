# Capability gaps — notebook-x — 2026-08-09

2 genuine capability gaps flagged today. Internal office notes — not GitHub Issues, not customer-facing.

## The Trainee — case `qa-2026-w07-d1-094`

[שגיאה בניסוח הדוח: Too many subrequests by single Worker invocation. To configure this limit, refer to https://developers.cloudflare.com/workers/wrangler/configuration/#limits] Notebook-X's kb-linux notebook returned no answer at all for this question. שאלה: Great, a new case! I'm excited to learn.

Okay, so the case is "Writing a bash script that safely handles a failed mid-job lock file." That means we're dealing with a situation where a nightly backup job fails and then tries to start again, but can't because the lock file from the previous run is still there.

Hmmm... To clarify, you want me to write a bash script that will:

1. Check if the lock file exists before starting the job?
2. If the lock file exists, what should the script do? Should it restart the job after the previous one is fully cleaned up?
3. Are there any specific bash commands or patterns that I should use to achieve this?

And, I'd love to have a detailed step-by-step guide on how to implement this. Can you provide a PDF or some written instructions on how to write this script? I'd like to make sure I get it right.

Also, just to confirm, this script will be run on a Linux machine, right?

Oh, and one more thing, what are the possible reasons why the lock file might not be deleted when the previous job is killed mid-run? Is it because the process that created the lock file didn't properly clean up after itself?

---

## The Productive — case `qa-2026-w07-d1-172`

[שגיאה בניסוח הדוח: Too many subrequests by single Worker invocation. To configure this limit, refer to https://developers.cloudflare.com/workers/wrangler/configuration/#limits] Notebook-X's kb-cloud-devops notebook returned no answer at all for this question. שאלה: Managing environment-specific config across dev/staging/prod. Give me the short, fast, accurate fix. What is a clean pattern for managing environment-specific configuration and secrets across dev, staging, and production without duplicating files?
